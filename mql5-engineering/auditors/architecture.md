# Architecture auditor

Use engineering/codebase-design.md. Look for God handlers, scattered state, duplicated trade/risk logic, shallow wrappers, missing test seams, execution decisions across many callers, speculative abstraction, and leaked implementation knowledge. Architecture findings require concrete friction or risk, not style preference.
