# UI auditor

Apply only when UI exists. Verify controls, states, labels, numerical values, pause/start/close semantics, error/connectivity feedback, stale data, and design-source consistency. Compare displayed state, configured state, and actual trading state.
